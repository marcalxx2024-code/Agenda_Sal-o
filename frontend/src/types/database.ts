// The generated schema stays at the repository root so backend and frontend
// share one source of truth. Do not recreate database row types here.
export type { Database, Json } from '../../../database.types'
